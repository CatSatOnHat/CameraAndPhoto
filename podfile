# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CameraAndPhoto' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
pod 'BlocksKit', '~> 2.2.5'
  # Pods for CameraAndPhoto

  target 'CameraAndPhotoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CameraAndPhotoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
